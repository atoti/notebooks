{
  "name": "application-extension",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build": "lerna run build",
    "build-package": "lerna run build --include-dependencies --scope"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.5.2",
    "ts-node": "^10.0.0",
    "yarn-deduplicate": "^3.1.0"
  },
  "engines": {
    "node": ">= 16.6.0",
    "yarn": ">= 1.22.10"
  }
}
